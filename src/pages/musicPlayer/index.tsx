import { FC } from 'react';
import style from './index.module.scss'

const MusicPlayer: FC = () => {
  return <div className={style.musicPlayer}>
    musicPlayer
  </div>
}

export default MusicPlayer;